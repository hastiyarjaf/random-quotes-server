# Server
NODE_ENV=development
PORT=3000
ALLOWED_ORIGIN=http://localhost:8080

# General rate limiting
RATE_LIMIT_WINDOW_MS=900000      # 15 minutes
RATE_LIMIT_MAX=100               # max 100 requests per window per IP

# Chat endpoint rate limiting
CHAT_RATE_LIMIT_WINDOW_MS=300000 # 5 minutes
CHAT_RATE_LIMIT_MAX=20           # max 20 chat requests per window per IP

# AI configuration
# Set AI_PROVIDER to 'gemini' to use Google Gemini (default is gemini when key is present)
AI_PROVIDER=gemini

# Your Google AI Studio API key (https://aistudio.google.com/app/apikey)
# Leave empty to use intelligent fallback responses
GEMINI_API_KEY=

# Optional: override model (defaults to gemini-1.5-flash)
GEMINI_MODEL=gemini-1.5-flash